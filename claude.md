NEVER delete or modify a DB without explicit orders.

never commit without explicit user command. 

if running py always activate a venv. search for a venv called .venv or venv. 

/Users/philipgalebach/coding-projects/buenosaires_properties/venv